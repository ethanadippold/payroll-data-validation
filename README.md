# payroll-data-validation
Python script to validate payroll data and detect formatting and calculation errors

## Project Overview
This project validates payroll data by parsing structured text files, extracting pay components, and verifying calculated totals.

## What This Demonstrates
- File I/O and string parsing in Python
- Data validation and error handling
- Applying business rules to detect inconsistencies
